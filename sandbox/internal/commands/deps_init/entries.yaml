identifiers: ["deps-init"]
category: Dependency System
help: Initializes the dependency-injection subsystem for the project
long-description: |
  Creates the sandbox/deps and adapters directories and calls build.
  Run this once before using dep-install.
examples:
  - "deps-init"
  - "deps-init --path ./my-project"
flags:
  - name: path
    identifiers: ["--path"]
    description: the dir holding the project (defaults to the current directory)
    examples:
      - "deps-init --path ./my-project"
    type: string
    default: "."
  - name: quiet
    identifiers: ["--quiet", "-q"]
    description: Quiets the cli output
    examples:
      - "deps-init -q"
    type: boolean
