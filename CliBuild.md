## Cli Build

### cli-init
command:
~~~bash
agnos cli-init
~~~
workflow:
- install the argv deps
- renders the group cli.


### cli-purge
command:
~~~
bash
agnos cli-purge
~~~
workflow:
- removes all the files of the cli group