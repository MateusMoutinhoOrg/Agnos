## Objective: 
Remove all yamls of sandbox/dir  by replacing for comptime importing 


## Comptime Importing 
for each file-generated that requires commands,api or routes, instead of using parsers, and yamls , use a comptime executoor,
Basicly you create a temp file,import the sandbox lib( Equal to cmd/main/main.go)retrives the information you need from api,(commands) ,api elements,etc, after write the docs, files,etch.