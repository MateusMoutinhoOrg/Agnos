paths:
  -
    idendentifier: "article"

  - 
    name: "article-name"


headers:
  - name : "username"
    type: string
    required: true

params:
  # in these case, since  the username is present in both headers and params,
  # it will consider the only first 
  - name: "username" 
    type: string
    required: true

examples:
  - "curl -X GET http://[IP_ADDRESS]/get-user-by-username\n    -H 'username: mateus'"
  


    