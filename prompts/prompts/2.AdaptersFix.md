using [boost_adapter](/bootstrap/adapters/standard/standard.go) as sample, adapt these project adapters to have the same factory style i did on boost_adapter. the ideia is that all required objects, be constructed using factorys.