using [boost_adapter](/bootstrap/adapters/standard/standard.go) as sample, adapt these project adapters to have the same factory style, without the needs of adapters object, only using clojure constructions.