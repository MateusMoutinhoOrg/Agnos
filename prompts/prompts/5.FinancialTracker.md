change the hole lib, to be a financial traccker lib
it must be able to add category, add speend and recived transactions, list transactions,etc ... 
Note that you must remove the current functions. and rebuild the whole lib, also use the deps database to create and persist data.